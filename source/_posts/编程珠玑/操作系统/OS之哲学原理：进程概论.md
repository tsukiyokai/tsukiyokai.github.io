---
title: OS之哲学原理：进程概论
abbrlink: 9be380c3
categories:
  - 编程珠玑
  - 操作系统
date: 2021-03-14 20:46:00
---

## 4.1 进程概论

进程从根本上说是操作系统对 CPU 进行的抽象和装扮。

因为要并发，所以我们发明了进程。进程让每个用户感觉到自己独占 CPU。因此，进程就是为了在 CPU 上实现多道编程而出现的概念。

> 进程=程序+执行

## 4.2 进程模型

- 物理视角 进程切换：任意时刻 CPU 只能执行一条指令，因此任意时刻只有一个进程执行。
- 逻辑视角 多道并发：每个进程可以执行也可以暂时挂起。
- 时序视角 持续推进：每个进程必须往前推进。“人不能两次踏进同一条河流。”

## 4.3 多道编程

人发明进程是为了多道编程，多道编程是为了提高 CPU 的效率。比起单道编程，多道编程可以显著改善响应时间。

多道编程度数和 CPU 利用率的关系：CPU 利用率随着度增加而增加，直到一个临界点。

## 4.4 进程的产生与消亡

产生：

- 系统初始化（神创造人）
- 正在执行的程序执行进程创立程序（人生子）
- 用户请求创立新进程（试管婴儿）

消亡：

- 寿终：进程运行完成而退出（自愿退出）
- 自杀：进程发生错误自行退出（自愿退出）
- 他杀：进程被其他进程终止（非自愿退出）
- 处决：进程因异常或非法操作被操作系统强行终结（非自愿退出）

## 4.6 进程的状态

进程可以执行，也可以挂起。根据挂起的原因不同，又将挂起分为阻塞和就绪。

因此将进程分为了三种状态：执行、阻塞和就绪。

- 执行中的进程（执行态）因等待输入阻塞而主动挂起，进入阻塞态。
- 阻塞中的进程（阻塞态）因其等待的资源到达而不再阻塞，进入就绪态。
- 已就绪的进程（就绪态）因获得 CPU 控制权，得以重新进入执行态；

执行中的进程（执行态）也会因执行时间太久，为公平起见被操作系统挂起让其他进程执行，进入就绪态。

总结：

- 阻塞这种挂起是进程自身的原因（内因），此时即使把 CPU 控制权交给它也不会运行；而就绪这种挂起是操作系统的原因（外因），进程自己并无问题，只要重新把 CPU 控制权交给进程就立即可以运行。
- 阻塞态无法直接转化为执行态，必须先转化为就绪态。因为阻塞进程即使被给予 CPU 也无法执行，因此操作系统在进程调度时只需查看就绪的进程，而无需浪费时间查看阻塞的进程。
- 一个进程只有在执行时才可能阻塞，就绪态无法直接转化成阻塞态。因为处于就绪态的进程并未执行，自然无法进入阻塞态。就像一个人停滞不前，自然不会有人成为他的障碍。

## 4.7 进程与地址空间

进程空间也称地址空间。地址空间就是进程要用的所有资源。做个比喻就是：音乐剧的舞台布景及设施就是地址空间，而舞台上的男女演员是线程。进程与地址空间研究的主要内容就是如何安全高效地让所有进程共享这片物理内存。

## 4.8 进程管理

当一个进程产生时，操作系统需要为其创建记录。操作系统用于维护进程记录的结构就是进程表或进程控制块（PCB）。进程表保持在操作系统所在的内核空间而非用户空间里。
