---
title: C++多态
abbrlink: 75300d42
categories:
  - 编程珠玑
  - CPP
tags:
  - OOP
date: 2021-04-27 12:22:39
---

## 什么是多态？

多态（英语：polymorphism）指为不同数据类型的实体提供统一的接口。

## 为什么需要多态？

1. 封装可以隐藏实现细节，使代码模块化；
2. 继承可以拓展已存在的代码模块，实现代码重用；
3. 多态除了实现代码的复用外，还可以解决项目中紧耦合的问题，提高程序的可拓展性。

## 多态的实现

- 动态多态：也称运行时多态，通过类继承机制和虚函数机制生效于运行期。可以优雅地处理异质对象集合，只要其共同的基类定义了虚函数的接口。也被称为子类型多态或包含多态。在面向对象程序设计中，这被直接称为多态。

- 静态多态：也称编译时多态。静态多态主要是重载，包括函数重载和运算符重载。模板允许将不同的特殊行为和单个泛化记号相关联，由于这种关联处理于编译期而非运行期，因此被称为“静态”。可以用来实现类型安全、运行高效的同质对象集合操作。C++STL 不采用动态多态来实现就是个例子。
