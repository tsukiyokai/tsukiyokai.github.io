---
title: 循环链表的遍历
abbrlink: 83968b7
categories:
  - 编程珠玑
  - 数据结构
tags:
  - 链表
date: 2021-02-11 01:26:00
---

# 循环链表 遍历

In a conventional linked list, we traverse the list from the head node and stop the traversal when we reach NULL. In a circular linked list, we stop traversal when we reach the first node again.

```c++
void printList(Node* head) {
    Node* temp = head;
    if (head != NULL) {
        do {
            cout << temp->data << " ";
            temp = temp->next;
        } while (temp != head);
    }
    cout << endl;
}
```

因为初始时temp=head，编译器无法分辨此时是否是temp第二次追上head，所以要用do-while语句，在循环判断前先执行一轮。

## 参考资料

https://www.geeksforgeeks.org/circular-linked-list-set-2-traversal/