---
title: 双端队列的双链表实现
abbrlink: 42cde6b5
categories:
  - 编程珠玑
  - 数据结构
tags:
  - 队列
  - 链表
date: 2021-02-14 00:46:00
---

头————尾（allows insert and delete at both ends）

## 主要功能

```
insertFront() : Adds an item at the front of Deque.
insertRear()  : Adds an item at the rear of Deque.
deleteFront() : Deletes an item from front of Deque.
deleteRear()  : Deletes an item from rear of Deque.

getFront() : Gets the front item from queue.
getRear()  : Gets the last item from queue.
isEmpty()  : Checks whether Deque is empty or not.
size()     : Gets number of elements in Deque.
erase()    : Deletes all the elements from Deque.
```

![img](https://media.geeksforgeeks.org/wp-content/uploads/deque_dll.jpg)

## 伪码

**Insertion at Front end**

```
Allocate space for a newNode of doubly linked list.
IF newNode == NULL, then
    print "Overflow"
ELSE
    IF front == NULL, then
        rear = front = newNode
    ELSE
      newNode->next = front
      front->prev = newNode
      front = newNode
```

**Insertion at Rear end**

```
Allocate space for a newNode of doubly linked list.
IF newNode == NULL, then
    print "Overflow"
ELSE
    IF rear == NULL, then
        front = rear = newNode
    ELSE
        newNode->prev = rear
        rear->next = newNode
        rear = newNode
```

**Deletion from Front end**

```
IF front == NULL
    print "Underflow"
ELSE
    Initialize temp = front
    front = front->next
    IF front == NULL
        rear = NULL
    ELSE
        front->prev = NULL
    Deallocate space for temp
```

**Deletion from Rear end**

```
IF front == NULL
    print "Underflow"
ELSE
    Initialize temp = rear
    rear = rear->prev
    IF rear == NULL
        front = NULL
    ELSE
        rear->next = NULL
    Deallocate space for temp
```
