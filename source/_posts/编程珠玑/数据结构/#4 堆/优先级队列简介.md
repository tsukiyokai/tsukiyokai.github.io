---
title: 优先级队列简介
abbrlink: 7a91
categories:
  - 编程珠玑
  - 数据结构
tags:
  - 队列
  - 堆
date: 2021-04-17 02:51:26
---

## 优先级队列的概念

优先级队列是具有以下属性的队列的扩展：

1. 每个元素都有与之关联的优先级。
2. 高优先级元素在低优先级元素之前出队。
3. 相同优先级的元素按照在队列中的顺序进行操作。

## 优先级队列的操作

典型的优先级队列支持以下操作：
`insert(item, priority)` 插入具有指定优先级的项
`getHighestPriority()` 返回优先级最高的项
`deleteHighestPriority()` 删除优先级最高的项

## 优先级队列的实现

### 使用数组

一个简单的实现途径是使用具有以下结构的数组。
```c++
struct item {
    int item;
    int priority;
}
```
使用数组时，插入操作复杂度为O(1)，取最高优先级元素的操作和删除最高优先级元素的操作复杂度都为O(n)。如果使用链表代替数组，插入和取的复杂度不变，删除操作变得简单一些，因为不用大面积移动元素。

### 使用堆

堆通常是优先队列实现的首选，因为堆提供了比数组或链表更好的性能。在二叉堆中，getHighestPriority()可以在O(1)时间内实现，insert()可以在O(logn)时间内实现，deleteHighestPriority()也可以在O(logn)时间内实现。
更进一步，对于Fibonacci堆，insert()和getHighestPriority()都可以在O(1)摊销时间内实现，deleteHighestPriority()可以在O(logn)摊销时间内实现。

补充一点，算法中的amortized time，即摊销时间，它和平均时间是略有不同的。
定义：Amortized time is the way to express the time complexity when an algorithm has the very bad time complexity only once in a while besides the time complexity that happens most of time.

## 优先级队列的应用

1. CPU调度
2. 图算法，如Dijkstra最短路径算法，Prim最小生成树算法等
3. 所有涉及优先级的队列应用

## 参考资料

https://www.geeksforgeeks.org/priority-queue-in-cpp-stl/