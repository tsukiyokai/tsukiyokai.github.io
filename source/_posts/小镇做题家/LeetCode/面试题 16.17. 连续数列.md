---
title: 面试题 16.17. 连续数列
abbrlink: baa8
categories:
  - 小镇做题家
  - LeetCode
tag: DP
date: 2021-05-04 21:37:11
---
给定一个整数数组，找出总和最大的连续数列，并返回总和。

示例：

输入：[-2,1,-3,4,-1,2,1,-5,4]
输出：6
解释：连续子数组 [4,-1,2,1] 的和最大，为 6。
进阶：如果你已经实现复杂度为 O(n) 的解法，尝试使用更为精妙的分治法求解。

```c++
class Solution {
public:
    int maxSubArray(vector<int>& nums) {
        if (nums.size() == 0) return 0;
        int maxsum = nums[0]; // 最大和
        int temp = nums[0]; // 当前和
        for (int i = 1; i < nums.size(); ++i) {
            temp += nums[i];
            if (temp < nums[i]) { // 说明此前和为负数且当前数为正数
                temp = nums[i]; // 舍弃此前和
                maxsum = max(nums[i], maxsum);
            }
            else maxsum = max(temp, maxsum);
        }
        return maxsum;
    }
};
```

还是跟LC53是同一题...