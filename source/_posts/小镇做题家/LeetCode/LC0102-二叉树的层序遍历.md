---
title: LC102 二叉树的层序遍历
categories:
  - 小镇做题家
  - LeetCode
abbrlink: 3e6b56c6
tags:
  - 树
  - 队列
date: 2021-04-07 00:38:26
---

## 题目

给你一个二叉树，请你返回其按层序遍历得到的节点值。
即逐层地从左到右访问所有节点。

```tex
输入：
    3
   / \
  9  20
    /  \
   15   7

输出：
{
  [3],
  [9,20],
  [15,7]
}
```

## 解法：队列

```c++
class Solution {
public:
    vector<vector<int>> levelOrder(TreeNode* root) {
        vector<vector<int>> res;
        if (!root) return res;
        queue<TreeNode*> q;
        q.push(root);
        while (!q.empty()) {
            vector<int> tmp;
            int len = q.size();
            for (int i = 0; i < len; i++) {
                TreeNode* node = q.front();
                q.pop();
                tmp.push_back(node->val);
                if (node->left) q.push(node->left);
                if (node->right) q.push(node->right);
            }
            res.push_back(tmp);
        }
        return res;
    }
};
```
