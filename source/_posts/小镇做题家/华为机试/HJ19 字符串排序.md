---
title: HJ19 字符串排序
categories:
  - 小镇做题家
  - 华为机试
abbrlink: 48639b35
tags:
  - 字符串
  - 排序
date: 2021-05-01 03:36:57
---

给定n个字符串，请对n个字符串按照字典序排列。
输入描述: 输入第一行为一个正整数n(1≤n≤1000),下面n行为n个字符串(字符串长度≤100),字符串中只含有大小写字母。
输出描述: 数据输出n行，输出结果为按照字典序排列的字符串。

```tex
输入：
9
cap
to
cat
card
two
too
up
boat
boot
输出：
boat
boot
cap
card
cat
to
too
two
up
```

## 解法

```c++
#include <iostream>
#include <vector>
#include <string>
#include <algorithm>
using namespace std;

int main() {
    int n;
    vector<string> v;
    cin >> n;
    while (n--) {
        string str;
        cin >> str;
        v.push_back(str);
    }
    sort(v.begin(), v.end(), cmp);
    for (auto word : v)
        cout << word << endl;

    return 0;
}
```

一个坑是while (n--)里面的cin >> str，我本来写的是getline 结果发现总是输入输出有问题。我突然想起来以前的笔记，cin>>和getline混用时的大坑。但是这里比那个写的还要复杂一点，因为不是严格混用。最后我还是改成cin>>str了，有时间再慢慢研究getline到底怎么用