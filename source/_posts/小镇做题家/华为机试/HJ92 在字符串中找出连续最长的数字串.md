---
title: HJ92 在字符串中找出连续最长的数字串
categories:
  - 小镇做题家
  - 华为机试
abbrlink: 41e088d6
tags:
  - 字符串
date: 2021-05-02 17:49:39
---

输入一个字符串，返回其最长的数字子串，以及其长度。若有多个最长的数字子串，则将它们全部输出（按原字符串的相对位置）
本题含有多组样例输入。

```c++
#include<iostream>
#include<string>
#include<sstream>
#include<vector>
#include<algorithm>
using namespace std;

bool cmp(string s1, string s2) {
    return s1.length() > s2.length();
}

int main() {
    string s;
    while (cin >> s) {
        for (auto& c : s)
            if (!isdigit(c)) c = ' ';

        stringstream ss(s);
        string tmp;
        vector<string> v;
        while (ss >> tmp)
            v.push_back(tmp);

        sort(v.begin(), v.end(), &cmp);
        size_t len = v[0].length();
        for (size_t i = 0; v[i].length() == len; i++)
            cout << v[i];

        cout << "," << len << endl;
    }
    return 0;
}
```

我这个思想非常巧妙
先让
`abcd12345ed125ss123058789`
变成
`····12345··125··123058789`
然后把它通过 stringstream 传进一个字符串数组里，对长度进行排序，最后输出
