---
title: HJ102 字符统计
categories:
  - 小镇做题家
  - 华为机试
abbrlink: deea
tags:
  - 字符串
  - 哈希
  - 排序
date: 2021-04-30 22:25:42
---
## 题目

输入一个只包含小写英文字母和数字的字符串，按照不同字符统计个数由多到少输出统计结果，如果统计的个数相同，则按照ASCII码由小到大排序输出。
本题含有多组样例输入

输入：
一个只包含小写英文字母和数字的字符串。
输出：
一个字符串，为不同字母出现次数的降序表示。若出现次数相同，则按ASCII码的升序输出。

## 解法：map+pair+stabl_sort

```c++
#include <iostream>
#include <vector>
#include <map>
#include <algorithm>
#include <string>

using namespace std;
bool isBigger(const pair<char, int>& a, const pair<char, int>& b) {
    return a.second > b.second;
}
int main() {
    string str;

    while (getline(cin, str)) {
        map<char, int> m;
        for (auto c : str)
            ++m[c];
        vector<pair<char, int> > v(m.begin(), m.end());
        stable_sort(v.begin(), v.end(), isBigger);

        for (auto& p : v)
            cout << p.first;
        cout << endl;
    }
    return 0;
}
```

这道题有个很大的坑，就是stable排序中比较函数的参数，如果不加const会造成比较严重的编译错误，具体原因现在还是不太清楚。
谷歌了一遍，没什么有价值的解答，可以权且参考这两个：
https://en.cppreference.com/w/cpp/algorithm/stable_sort
https://stackoverflow.com/questions/61892533/why-arguments-of-comparator-function-of-stdstable-sort-must-be-set-constants?noredirect=1&lq=1
自己能做的就是尽可能加上const吧，反正到目前为止，能加const的地方，加上去不会有什么报错，但是不加的话有时候错误还挺奇怪的。。。

这题也算是pair比较合适的应用了，别的地方用的还挺少的，当个典型来学习吧

知乎：map的元素是pair。pair的数据成员是public的，两个成员分别命名为first和second，用成员访问符来访问即可