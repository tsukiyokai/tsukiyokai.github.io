---
title: JZ49 丑数
abbrlink: 96df
date: 2021-04-15 02:37:10
categories: [小镇做题家,剑指 Offer]
tags: [堆,动态规划]
---
## 题目

我们把只包含质因子2,3和5的数称作丑数。求按从小到大的顺序的第n个丑数。

## 解法1：暴力法

维护一个计数器cnt，同时对途经的所有数字都判断其是否为丑数。

**代码**

```c++
#include<bits/stdc++.h>
using namespace std;

bool isUgly(int n) {
    if (n < 1) return false;
    while (n % 2 == 0) n /= 2;
    while (n % 3 == 0) n /= 3;
    while (n % 5 == 0) n /= 5;
    return n == 1;
}

int getNthUglyNo(int n) {
    int i = 1;
    int cnt = 1;
    while (cnt < n) {
        i++;
        if (isUgly(i)) cnt++;
    }
    return i;
}

int main() {
    unsigned no = getNthUglyNo(150);
    printf("150th ugly num is %d\n", no);
    return 0;
}
```

## 解法2：动态规划

**大体思路**

暴力法的思想是：数域上原本就存在无穷多个丑数，我们只需要找出这些丑数，具体做法是一个个判断。

DP的思想是：世界上只存在第一个丑数1和产生丑数的做法，即乘2乘3乘5，每一个丑数的来源分为这三种，且可以混合这些操作，但终究还是分三种。你需要利用规律来生产符合要求的丑数。

这是一种空间复杂度为O(n)的省时解决方案。丑数序列是：1,2,3,4,5,6,8,9,10,12,15,...因为每个数字只能被2,3,5整除，于是一种查看丑数序列的方法就是将序列分成三组，如下所示：
(1) 1×2, 2×2, 3×2, 4×2, 5×2, …
(2) 1×3, 2×3, 3×3, 4×3, 5×3, …
(3) 1×5, 2×5, 3×5, 4×5, 5×5, …
可以发现每个子序列都是丑序列本身（1,2,3,4,5,...）乘以2,3,5。然后我们使用和归并排序类似的归并方法来从三个子序列中获得每一个丑数。每一步我们都选择最小的那个，然后再向前迈进一步。

**具体算法**

```
1 Declare an array for ugly numbers:
    ugly[n];
2 Initialize first ugly no:
    ugly[0] = 1;
3 Initialize three array index variables i2, i3, i5 to point to 1st element of the ugly array:
    i2 = i3 = i5 = 0;
4 Initialize 3 choices for the next ugly no:
    next_mulitple_of_2 = ugly[i2]*2;
    next_mulitple_of_3 = ugly[i3]*3;
    next_mulitple_of_5 = ugly[i5]*5;
5 Now go in a loop to fill all ugly numbers till 150:
    For (i = 1; i < 150; i++) {
        next_ugly_no = Min(next_mulitple_of_2, next_mulitple_of_3, next_mulitple_of_5);
        ugly[i] = next_ugly_no;
        
        if (next_ugly_no == next_mulitple_of_2) {
            i2 = i2 + 1;
            next_mulitple_of_2 = ugly[i2]*2;
        }

        if (next_ugly_no == next_mulitple_of_3) {
            i3 = i3 + 1;
            next_mulitple_of_3 = ugly[i3]*3;
        }

        if (next_ugly_no == next_mulitple_of_5) {
            i5 = i5 + 1;
            next_mulitple_of_5 = ugly[i5]*5;
        }
    } /* end of for loop */
6 return next_ugly_no
```

**流程推理**

```
initialize
    ugly[] = 1
    i2 = i3 = i5 = 0;

First iteration
    ugly[1] = Min(ugly[i2]*2, ugly[i3]*3, ugly[i5]*5)
            = Min(2, 3, 5)
            = 2
    ugly[] = 1 2
    i2 = 1, i3 = i5 = 0 (i2 got incremented)

Second iteration
    ugly[2] = Min(ugly[i2]*2, ugly[i3]*3, ugly[i5]*5)
            = Min(4, 3, 5)
            = 3
    ugly[] = 1 2 3
    i2 = 1, i3 = 1, i5 = 0 (i3 got incremented)

Third iteration
    ugly[3] = Min(ugly[i2]*2, ugly[i3]*3, ugly[i5]*5)
            = Min(4, 6, 5)
            = 4
    ugly[] = 1 2 3 4
    i2 = 2, i3 = 1, i5 = 0 (i2 got incremented)

Fourth iteration
    ugly[4] = Min(ugly[i2]*2, ugly[i3]*3, ugly[i5]*5)
            = Min(6, 6, 5)
            = 5
    ugly[] = 1 2 3 4 5
    i2 = 2, i3 = 1, i5 = 1 (i5 got incremented)

Fifth iteration
    ugly[4] = Min(ugly[i2]*2, ugly[i3]*3, ugly[i5]*5)
            = Min(6, 6, 10)
            = 6
    ugly[] = 1 2 3 4 5 6
    i2 = 3, i3 = 2, i5 = 1 (i2 and i3 got incremented)

Will continue same way till I < 150
```

i2,i3,i5迭代的依据是：确保既不跳过任何一个丑数，也不产生哪怕一次重复。


## 参考资料

https://www.geeksforgeeks.org/ugly-numbers/