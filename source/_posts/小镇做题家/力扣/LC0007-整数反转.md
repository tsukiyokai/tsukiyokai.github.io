---
title: LC7 整数反转
abbrlink: 4025
categories:
- 小镇做题家
- LeetCode
tag: 字符串
date: 2021-02-22 00:00:00
---

## 题目

给你一个32位的有符号整数x，返回x中每位上的数字反转后的结果。
如果反转后整数超过32位的有符号整数的范围[−2^31, 2^31−1]，就返回0。

## 解法：字符串

**代码**

```c++
class Solution {
public:
    long long reverse(int x) {
        long long res = 0;
        x >= 0 ? res = reverseu(x) : res = (-1) * reverseu(abs(x));
        return res > INT_MAX || res < INT_MIN ? 0 : res;
    }

    long long reverseu(unsigned x) {
        string num = to_string(x);
        string num2 = num;
        for (unsigned i = 0; i < num.size(); ++i) {
            num2[i] = num[num.size() - i - 1];
        }
        long long inum2 = stoll(num2);
        return inum2;
    }
};
```

**注释**

一开始字符串转整数用的是stoi()，运行正常但是提交报错，原因是有一个越界的测试用例1534236469，改成stoll()后顺利通过。